import React, { Component } from 'react';
import Section from './Section';

class ProfileCard extends Component {
  render() {
    return (
      <Section area='profile'>
        ProfileCard
      </Section>
    );
  }
}

export default ProfileCard;